export declare class Group {
    id: number;
    name: string;
    members: number[];
    expenses: number[];
    createdAt: Date;
    updatedAt: Date;
    constructor(id: number, name: string, members: number[], expenses: number[]);
}
